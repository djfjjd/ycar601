{
  "name": "ycar601-vehicle-management",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test",
    "lint": "node --check src/main.js && node --check src/parking-layouts.js && node --check src/parking-map.js && node --check src/validation.js && node --check 'functions/api/[[path]].js' && node --check functions/_lib/google-sheets.js"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  },
  "dependencies": {
    "web-push-neo": "^0.1.2"
  }
}
